{
  "name": "hanik-mcp-server",
  "version": "1.1.0",
  "description": "Minimal MCP server that returns 'test hanik mcp ok'. Deployable to Azure via Bicep (Container Apps).",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@azure/msal-browser": "^2.39.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.2",
    "multer": "^2.2.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/multer": "^2.2.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
